# FRP Prototype Environment Variables
NODE_ENV=development
PORT=3001
API_PREFIX=/api/v1

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_db_password_here
DB_NAME=frp_prototype

# Logging Configuration
LOG_LEVEL=info

# Optional: Email Configuration (for notifications)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
